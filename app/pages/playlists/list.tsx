import { Container } from '@radix-ui/themes';

import { DataTable } from './list.table';

const PlaylistsList = () => {
  return (
    <Container>
      <DataTable />
    </Container>
  );
};

export default PlaylistsList;
